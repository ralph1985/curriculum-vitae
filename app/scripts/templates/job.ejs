<div>
    <h2><%= job.name %></h2>

    <p><%= job.description %></p>

    <div>
        <ul>
            <% job.projects.each(function(project) { %>
            <li>
                <a href="#jobs/<%= job.id %>/<%= project.id %>">
                    <%= project.get('name') %>
                </a>
            </li>
            <% }); %>
        </ul>
    </div>

    <div id="project-content"></div>
</div>
